import { NumbergSortPipePipe } from './numberg-sort-pipe.pipe';

describe('NumbergSortPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NumbergSortPipePipe();
    expect(pipe).toBeTruthy();
  });
});
